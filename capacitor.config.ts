import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneybhai.app',
  appName: 'MoneyBhai',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
